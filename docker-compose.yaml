version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://presence_user:presence_pass@db:5432/presence_db
      - SECRET_KEY= a9F3kL8zQ1mXv7P2tR5uYwC0bN6HsD4e
      - CLOUDINARY_CLOUD_NAME=dqxxybf4c
      - CLOUDINARY_API_KEY=499997679123446
      - CLOUDINARY_API_SECRET=rEfOCVh661yoBiz4X4IQJ-Nc-rE
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${dratnesh441@gmail.com}
      - SMTP_PASSWORD=${utcs jzhf lvck fnuw}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=presence_user
      - POSTGRES_PASSWORD=presence_pass
      - POSTGRES_DB=presence_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U presence_user -d presence_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
